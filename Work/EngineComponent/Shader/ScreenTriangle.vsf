struct PSInput {
    float4 _position : SV_Position;
};

PSInput main(uint vertexId : SV_VertexId)
{
    PSInput result;
    float x = vertexId % 3 == 0 ?  3 : -1;
    float y = vertexId % 3 == 2 ?  3 : -1;
    result._position = float4(x, y, 0, 1);

    return result;
}